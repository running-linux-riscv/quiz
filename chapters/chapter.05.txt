# 记录问题和 refs(作为问题的 tips)，但不可提供答案




== 补充问题

0） 开放问题：你在这一章学到的最重要，最有用的知识是什么？有不在你原来理解范围的知识吗(意料之外)？

1) readelf 所获取的入口地址，段头偏移量，是否是相对某个地址的？这些地址是加载地址、运行地址还是链接地址？

2) 82 页例 5-1 会无限循环，是为什么，如何解决？

3) .align 伪指令的第三个参数大于对齐值时，有意义吗？是否会出现多倍于对齐值对齐的情况？

4) 为什么用 .equ 分配的值被视作立即数，而 .word 保存的地址标签则被视为地址， .equ 分配的值的默认位数是多少？

5) 当各 .section 的 flags 未指明时，其默认值是否有规范进行规定，还是由汇编器进行判断？

6) 除了几个常用的 section 名称，更详细的 section 命名规范以及使用规则是什么？

7) 85页例5-2，前文说，使用相同的数字N。跳转指令只能引用（向后引用或者向前引用）最近定义的本地标签。而在例5-2中，第三行1： j 2f，为什么向后引用却没有向前引用。在这种例子的情况下，会向前还是向后引用？
